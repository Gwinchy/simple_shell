#include "shell.h"
/**
 * main - starting point
 * @argc: argument counter
 * @argv: argument vector
 * Return: Always 0 Success
 */
int main(int argc, char **argv)
{
	while (1)
	{
		prompt();
	}
	return (0);
}
